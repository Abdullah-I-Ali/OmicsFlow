% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omicsflow.R
\name{omicsflow}
\alias{omicsflow}
\title{Run the full OmicsFlow pipeline from R}
\usage{
omicsflow(
  rna = NULL,
  meth = NULL,
  cnv = NULL,
  snv = NULL,
  metadata = NULL,
  clinical = NULL,
  clinical_map = NULL,
  outdir = "results/myrun",
  n_top_rna = 200,
  n_top_meth = 500,
  n_top_cnv = 500,
  mofa_factors = 5,
  mofa_iter = 100,
  mofa_prefilter = 50,
  final_features = 20,
  cache = NULL,
  render_report = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{rna}{Path to RNA-seq matrix file (.rds or .csv), or NULL}

\item{meth}{Path to DNA methylation matrix file (.rds or .csv), or NULL}

\item{cnv}{Path to CNV data file (.rds, .csv, or .tsv), or NULL}

\item{snv}{Path to SNV data file (.rds, .csv, or .tsv), or NULL}

\item{metadata}{Path to sample metadata CSV file}

\item{clinical}{Path to clinical TSV/CSV file, or NULL}

\item{clinical_map}{Path to clinical mapping JSON file, or NULL}

\item{outdir}{Base output directory for results}

\item{n_top_rna}{Number of top variable genes/features to select for RNA}

\item{n_top_meth}{Number of top variable features to select for Methylation}

\item{n_top_cnv}{Number of top variable features to select for CNV}

\item{mofa_factors}{Number of factors to train in MOFA}

\item{mofa_iter}{Number of iterations for MOFA training}

\item{mofa_prefilter}{Number of top features based on MOFA weights to retain for survival prefiltering}

\item{final_features}{Number of final features to retain in clinical model}

\item{cache}{Path to CNV gene coordinates cache file. If NULL and configurations exist, defaults to configurations.}

\item{render_report}{Logical, whether to render the Quarto HTML report at the end}

\item{verbose}{Logical, whether to print detailed logs to console}
}
\value{
An S3 object of class \code{omicsflow_result} containing:
  \describe{
    \item{status}{Character, either "success" or "partial/failed"}
    \item{report_path}{Absolute path to the final Quarto report (if generated), else NULL}
    \item{output_dirs}{Named list of absolute paths to module output directories}
    \item{executed_modalities}{Character vector of executed components}
    \item{skipped_modalities}{Character vector of skipped components}
    \item{warnings}{Character vector of execution warnings}
    \item{errors}{Character vector of execution errors}
    \item{runtime}{Numeric execution time in seconds}
  }
}
\description{
Orchestrates the OmicsFlow analysis pipeline, dynamically executing preprocessing,
MOFA+ integration, machine learning survival analysis, and pathway enrichment
based on the provided subset of omics modalities. At least two modalities are
required for successful execution.
}
\examples{
\dontrun{
res <- omicsflow(
  rna = "data/rna.rds",
  meth = "data/meth.rds",
  metadata = "data/sample_metadata.csv",
  outdir = "results/subset_run"
)
}

}
