% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_inputs.R
\name{validate_inputs}
\alias{validate_inputs}
\title{Pre-flight validation of OmicsFlow pipeline inputs}
\usage{
validate_inputs(
  rna = NULL,
  meth = NULL,
  cnv = NULL,
  snv = NULL,
  metadata = NULL,
  clinical = NULL,
  clinical_map = NULL,
  cnv_cache = NULL
)
}
\arguments{
\item{rna}{Path to RNA-seq matrix file (.rds or .csv), or NULL}

\item{meth}{Path to DNA methylation matrix file (.rds or .csv), or NULL}

\item{cnv}{Path to CNV data file (.rds, .csv, or .tsv), or NULL}

\item{snv}{Path to SNV data file (.rds, .csv, or .tsv), or NULL}

\item{metadata}{Path to sample metadata CSV file (required for execution)}

\item{clinical}{Path to clinical TSV/CSV file, or NULL}

\item{clinical_map}{Path to clinical mapping JSON file, or NULL}

\item{cnv_cache}{Path to CNV gene coordinates cache file, or NULL for auto-detection}
}
\value{
An S3 object of class \code{omicsflow_validation} containing:
  \describe{
    \item{status}{Character, either "success" or "failed"}
    \item{valid}{Logical indicating whether all checks passed}
    \item{detected_modalities}{Character vector of executed/detected modules}
    \item{skipped_modalities}{Character vector of skipped modules}
    \item{messages}{Character vector of validation log messages}
    \item{warnings}{Character vector of validation warnings}
    \item{errors}{Character vector of validation errors}
    \item{runtime}{Numeric execution time in seconds}
  }
}
\description{
Performs comprehensive validation of user-prepared pipeline inputs including
file existence checks, modality counting (minimum 2 required), metadata
schema validation, sample ID alignment, and clinical column mapping
verification.
}
\examples{
\dontrun{
result <- validate_inputs(
  rna = "data/rna.rds",
  meth = "data/meth.rds",
  metadata = "data/sample_metadata.csv"
)
if (result$valid) message("Ready to run!")
}

}
